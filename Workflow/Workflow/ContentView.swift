//
//  ContentView.swift
//  Workflow
//
//  Created by LiasPub on 1/19/26.
//  This is an editing test by Michael Tufillaro
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
            Text("Michael Tufillaro 01/20/26")
            Text("I made this app. I can do other stuff with it too but idk what happens later if I unplug it and plug it back in lol.")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
